# Neon: pooled connection (used at runtime by the app)
DATABASE_URL=""
# Neon: direct connection (used by `prisma migrate` / `prisma db seed`)
DIRECT_URL=""

# 32+ random bytes, base64. Generate with:
# node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
AUTH_SECRET=""

# Password for the seeded therapist demo account (prisma/seed.ts)
SEED_THERAPIST_PASSWORD="MindFlow123!"
